; Copy next to StationApp.exe as settings.ini (the app creates one on first run).
[OverlaySettings]
; LAN address of the PC running the master controller.
MasterIP=192.168.1.10
; Shown in the master controller; use the number staff know the station by.
StationName=Station 1
AutoEnableOverlay=1
SessionEndTime=0
